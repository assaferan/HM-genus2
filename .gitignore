# Magma attach caches (auto-generated)
*.sig

# scratch / diagnostic scripts
_*.m

# editor / OS
.DS_Store

# Claude Code harness state
.claude/

# search output
search_results.txt
survivors.m

# LaTeX build artifacts
*.aux
*.log
*.out
*.synctex.gz

# run progress logs
*_progress.txt

# ES eigenvalue-cache transients (keep the merged es_eig/<label>.txt + PROGRESS.md)
es_eig/*.part.*
es_eig/chunk_*.log
es_eig/*.fast
hp_run.log

# diagnostic / run outputs (some older *_out.txt remain tracked from before this rule)
*_out.txt
*_out*.txt
search_results*.txt
email_ariel_draft.md
email_ariel_reply2.md
modularity_idx33.aux
modularity_idx33.log
modularity_idx33.out
email_ariel_reply3.md

# torsion-hmf run outputs
validate.out
sweep.out

# runtime locks / ES progress status (transient)
*.lock
es_eig/PROGRESS.md

# sweep auto-generated cutters (curated file is hecke_cutters.m)
hecke_cutters_generated.m
